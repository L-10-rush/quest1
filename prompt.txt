Inital prompt structure-
[1] PROMPT - give me a well structure to add my prompt in prompt.txt to document what i have achieved in this project as i can easily maintained

================================================================================
PROMPT LOG — video-dialogue-finder
================================================================================

================================================================================
GEN — Planning & Reasearch
================================================================================
[GEN-01]
MODEL    : Claude
CONTEXT  : Problem Statemen file- Problem statement - My mind rebels at stagnation.pdf
PROMPT   : i have only two day(24 - 26-aug 11:59pm) - the key thinks i have break the project
            problem statement so that i can work with individual module make it workable and best for module and also integrate with next stage of the module where the data flow seamlessly so that the basic at least work now -
1. getting the video( 
	1.1 either downloading with meta data or 
	1.2 getting the audio and have a time stamp mapping form the start time and end time and get the frame btw the time stamp or 
	1.3 last option taking the snapshot of 10 frame in a second ).
2. building the text extraction and pipeline and stored it into the file or any storage format which ever will be good and fast (suggest some idea for this module )
3. finding the first and best match of the dialogue and frame give me suggestion if  this is a problem statement and wanted to have some some research if this type of tech or any open source software exist or not just do research no need to write any code also video can be youtube link also

================================================================================
GEN — Architecture & Planning
================================================================================
[GEN-01]
MODEL    : Claude
PROMPT   : i will be more clear as i have video url without subtitle it have voice we can use of that i wanted to have it more modular like easily change language if needed in future scope. So give me clear picture what will be the best approach with details documentation with tradeoff of other approach and best approach also give me break up the problem statement into steps or sprint to complete it before the deadline (26 Aug) so to achieve workable projects solution

[GEN-02]
MODEL    : Claude
PROMPT   : Generate sprints breakdown of the Problem statement with detail structure explanation of the problem statement

[GEN-03]
MODEL    : Claude
PROMPT   : Give me the file skeleton structure with function defination and it todos comments make sure this criteria - SOLID-structured folder layout + module interfaces
OUTCOME  : Gave zip

================================================================================
S1 — Ingestion (video download + metadata)
================================================================================
[S1-01]
MODEL    : Claude
CONTEXT  : Sprint 1
PROMPT   : In the given URL there's no caption or hardsubs, only audio dialogue
  and conversation, so build audio-based for now. Add OCR plus audio
  cross-confirmation as a documented future scope. Also keep a JSON file
  per video URL, a separate folder for the frame images, and put word
  counts/frequencies and other metrics in the JSON.

[S1-02]
MODEL    : Claude
PROMPT   : For the ingestion layer I also want each downloaded video saved
           under a generated sequence name, and I want reruns against the
           same URL to skip the download entirely instead of fetching it
           again. Suggest a storage approach that keeps that lookup fast.
VERIFIED : Called the downloader twice against the same URL in a row. The
           second call never touched yt-dlp, it returned the cached
           VideoMetadata straight from the registry (confirmed via the
           "cache hit" log line), and the file on disk matched what was
           returned the first time.

================================================================================
S2 — Audio extraction
================================================================================
[S2-01]
MODEL    : Claude
PROMPT   : Ingestion is done. Now build the audio extraction stage: pull the
           audio out of the downloaded video with ffmpeg, in whatever
           format WhisperX/Vosk actually expect, plus a test for it.

================================================================================
S3 — Transcription (WhisperX word-level alignment)
================================================================================
[S3-01]
MODEL    : Claude
PROMPT   : Wire up WhisperX as the transcription engine. I need word-level
           timestamps, not just segment-level ones, since a segment can
           run several seconds and I need the exact frame. Also add Vosk
           behind the same interface as a lighter fallback, in case
           WhisperX turns out to be too heavy for the dev machine.

[S3-02]
MODEL    : Claude
PROMPT   : WhisperX already segments its output into sentence-level chunks
           during alignment and I'm throwing that away right now, only
           keeping the flat word list. Add a DialogueSegment type so the
           full transcript, every line spoken, in order, gets surfaced
           too, independent of whatever phrase is being searched for.
================================================================================
S4 — Fuzzy phrase matching
================================================================================
[S4-01]
MODEL    : Claude
PROMPT   : Build the phrase matcher. Slide a window over the transcript's
           words, score each window against my target phrase with
           RapidFuzz, and return the best-scoring span's start time. It
           has to tolerate small ASR mistakes, not require an exact match.

================================================================================
S5 — Frame location (timestamp → exact frame)
================================================================================
[S5-01]
MODEL    : Claude
PROMPT   : Once I have the match's start_time in seconds, convert it to a
           frame number using the video's own fps and seek OpenCV to that
           exact frame, then save it as a PNG. Handle the case where the
           seek doesn't land exactly on some codecs.


================================================================================
S6 — Output / storage
================================================================================
[S6-01]
MODEL    : Claude
PROMPT   : Wire up the last stage. Save the matched frame as a PNG and
           write a result.json per search with the timestamp, frame
           number, matched text, score, and the word-count/confidence
           metrics we talked about earlier. Keep the video-level metadata
           in its own file so repeated searches on the same video don't
           rewrite it every time.

================================================================================
INFRA — Docker, packaging, tooling
================================================================================
[INFRA-01]
MODEL    : Claude
PURPOSE  : Multi-stage Dockerfile with uv + ffmpeg
PROMPT   : Fix my docker-caompose.ymal and Dockerfile

================================================================================
DEBUG — Fixes made after something broke
================================================================================
[DEBUG-01]
MODEL    : Claude code
PROMPT   : Fix the Runing issues  with this - uv run python -m src.main \
  --url "https://ok.ru/video/248244667877" \
  --text "My mind rebels at stagnation"
